FROM openvino/ubuntu24_runtime:2026.0.0

WORKDIR /app
COPY hardware_probe_server.py probe_intel.py /app/
ENV REEFY_HARDWARE_PROBE=probe_intel
EXPOSE 8080
CMD ["python3", "/app/hardware_probe_server.py"]
